import 'package:flutter/material.dart';

class Second_Page extends StatelessWidget {
  const Second_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('This is the Second Page'));
  }
}
